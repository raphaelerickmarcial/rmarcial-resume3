<section class="resume-section p-3 p-lg-5 d-flex flex-column" id="interests">
        {% for entry in craft.entries.section('interest').find() %}
        <div class="my-auto">
          <h2 class="mb-5">Interests</h2>
          <p class="mb-0">{{entry.myInterest}}</p>
        </div>
        {% endfor %}
      </section>

    